function mostrar()
/*{
   tomo la edad ESTA ES LA FORMA FÂCIL XQ SOLO HAY QUE AGREGAR UN !
        var edad = document.getElementById("edad").value;
        
        if (!(edad >=13 && edad <= 17)) {
            alert("no sos adolescente");
        }
    }FIN DE LA FUNCIÓN*/

    {
        var edad = document.getElementById("edad").value;
        
        if (edad <13 || edad > 17) {
                 alert("no sos adolescente");
             }   
    }
